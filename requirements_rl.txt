# RL fine-tuning dependencies for ESPnet2 NeMo-aligned experiment
# Install with: pip install -r requirements_rl.txt

# WER computation (reward functions)
jiwer>=3.0.0

# LLM reward (Gemini API) — optional; mock fallback available without API key
google-generativeai>=0.5.0

# HuggingFace data loading (AfriSpeech-200, VoxPopuli, LibriSpeech)
# datasets 3.x dropped support for custom loading scripts (trust_remote_code).
# AfriSpeech-200 still uses afrispeech-200.py, so we must stay on 2.x.
datasets>=2.14.0,<3.0.0
huggingface_hub>=0.20.0

# Audio I/O for HF → kaldi WAV conversion
soundfile>=0.12.0
librosa>=0.10.0

# LoRA (uses ESPnet2's built-in loralib integration)
loralib>=0.1.2
